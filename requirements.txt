# ── CVD Accessibility Audit — Python Dependencies ────────────────────────────
#
# Install with:
#   pip install -r requirements.txt
#
# After installation, install Playwright browser binaries:
#   playwright install
# ─────────────────────────────────────────────────────────────────────────────

# Automated browser control — webpage scanning
playwright>=1.40.0

# Structured data handling — site lists, results storage
pandas>=2.0.0

# HTML parsing — DOM extraction
beautifulsoup4>=4.12.0

# HTTP requests — auxiliary page data retrieval
requests>=2.31.0

# Progress tracking — pipeline stage visualisation
tqdm>=4.66.0

# Debug visual overlays for detected components
opencv-python>=4.10.0
